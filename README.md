Pokédex

A small Pokédex built with **Vite + Vanilla JavaScript** consuming the public **PokeAPI**.  
You can search a Pokémon by name or ID and view its image, types, and base stats.

---

✨ Features
- Search by **name** or **ID**
- Display Pokémon **sprite**
- Show **types** with styled pills
- Base **stats** with colored progress bars
- Loading and error handling states

---

🛠️ Tech Stack
- HTML  
- CSS  
- JavaScript (Vanilla)  
- Vite  
- Fetch API ([PokeAPI](https://pokeapi.co/))

---

📦 Installation & Usage
```bash
# Clone the repository
git clone https://github.com/TomasPaulenas/pokedex-app.git

# Enter project folder
cd pokedex-app

# Install dependencies
npm install

# Run locally
npm run dev
